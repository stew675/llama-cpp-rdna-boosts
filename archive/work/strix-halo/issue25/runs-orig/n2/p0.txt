We need answer user: "Explain in detail how a hash map works internally, including collision handling, load factor, and resizing. Then write a complete implementation in Python."

Need produce final with detailed explanation and complete Python implementation. Need likely include open addressing vs chaining. User asks hash map internally, collision handling, load factor, resizing. Then complete implementation in Python. We can choose one implementation. Need be thorough. Could implement using separate chaining with linked list or arrays? Complete implementation in Python. Need maybe include tests? "complete implementation" likely class HashMap with get, put, delete, contains, keys, values, items, len, iter, resize. Need explain.

Need decide implementation. Python dict uses open addressing with perturb, but for educational implementation separate chaining is simpler and robust. But question includes collision handling, load factor, resizing. Could explain both and implement separate chaining. Or implement open addressing? Complete implementation in Python maybe more interesting with open addressing? Need ensure correctness. Let's think.

If implement separate chaining:
- internal table: list of buckets, each bucket maybe list of (key, value) or custom Node.
- hash: use built-in hash(key) & mask for power-of-two capacity. Need handle unhashable? raise TypeError.
- load factor = size / capacity. Resize when load factor > threshold (e.g. 0.75) or maybe when size > capacity * load_factor. Also shrink when load factor < 0.25? optional.
- collision handling: chaining: append to bucket list. Lookup scans bucket.
- resizing: allocate new capacity (double), rehash all entries.
- Need support key equality, not identity. Use ==.
- Need handle deletion: remove from bucket list, decrement size. Could use tombstones if open addressing; chaining no tombstones.
- Need handle updating existing key.
- Need handle None values? yes.
- Need handle keys that are unhashable: hash raises TypeError.
- Need handle hash collisions due to same hash but different keys.
- Need maybe handle negative hash: & mask works for power of two.
- Need capacity power of two. Initial capacity maybe 16.
- Need ensure capacity not zero.
- Need maybe implement __eq__? Not necessary but could.
- Need maybe implement __repr__.
- Need maybe implement __iter__, __contains__, __getitem__, __setitem__, __delitem__, __len__, keys, values, items, get, pop, setdefault, update, clear.
- Need complete enough.

Potential pitfalls:
- If using list of lists, deletion O(bucket length). Fine.
- Need avoid modifying during iteration? Could implement snapshot? Python dict iteration raises RuntimeError if modified. We can implement simple iterator over buckets and entries; if map modified during iteration, maybe raise RuntimeError. Need track version. Could implement version counter incremented on structural changes (put new, delete, resize, clear). Iterator stores version and checks. But if updating value of existing key, Python dict allows? Actually changing value during iteration is allowed? In Python, changing values of existing keys during iteration is allowed? Let's recall: dict iteration raises RuntimeError if dictionary size changes. Updating existing key value does not change size, so allowed. Our version should increment only on size changes (insert new, delete, clear, resize). But resize
