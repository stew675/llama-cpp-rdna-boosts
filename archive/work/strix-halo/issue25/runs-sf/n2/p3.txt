We need answer user: "Implement an LRU cache in Rust with O(1) get and put. Explain the data structure choices and show tests."

Need produce final with Rust code, explanation, tests. Need likely include HashMap and doubly linked list. Need O(1) get/put. Need maybe use std::collections::HashMap and custom doubly linked list with Node { key, value, prev, next } using Rc<RefCell<Node>>? But for O(1) and safe Rust, can use HashMap<K, NodeId> and intrusive linked list? Simpler: HashMap<K, (V, NodeIndex)> and VecDeque? But O(1) removal arbitrary requires doubly linked list. In Rust, can implement with HashMap<K, Node> where Node contains Rc<RefCell<...>>? Need avoid borrow issues.

Options:
1. Use `HashMap<K, Rc<RefCell<Node<V>>>>` and head/tail as Option<Rc<RefCell<Node<V>>>>. Node has key, value, prev, next. get: map.get(key) returns Option<&Rc<RefCell<Node>>>, clone Rc, lock RefCell, move_to_front. put: if exists update value and move_to_front; else insert new node at front, if capacity exceeded remove tail, map.remove(&tail.key). Need key stored in node to remove from map. This is straightforward but uses heap allocation per node and RefCell. O(1) amortized? HashMap O(1) average. Need mention average due to hashing.

Potential issue: In `get`, if we do `self.map.get(key)` then `node.borrow_mut()` while holding immutable borrow of map? We clone Rc before? `if let Some(node) = self.map.get(key) { let node = node.clone(); self.move_to_front(node); ... }` The `node` variable is Rc clone, no borrow of map after clone? Actually `self.map.get(key)` returns Option<&Rc<...>>; if we clone inside if let, the reference is alive during block? We can do:
```
let node = match self.map.get(key) { Some(n) => n.clone(), None => return None };
self.move_to_front(node.clone());
Some(node.borrow().value.clone())
```
But `node.borrow()` after move_to_front? move_to_front takes Rc and mutates. Need value clone. Could do `let value = node.borrow().value.clone(); self.move_to_front(node); Some(value)`. But if move_to_front borrows node, okay.

Need ensure no double borrow: `move_to_front` takes `Rc<RefCell<Node<V>>>` and uses `node.borrow_mut()`. If we call `node.borrow()` then drop guard before `move_to_front`. Good.

But if `V: Clone` for get returning owned value. Could also return `&V`? Hard due to RefCell and map. Usually LRU cache get returns Option<V> clone. If want no clone, can use `Ref`? But lifetime tied to RefCell, not easy with map. We'll specify `V: Clone`.

Need capacity zero? If capacity == 0, put should not store? get returns None. Need handle.

Need implement `new(capacity: usize)`. If capacity 0, map empty, head/tail None. put:
