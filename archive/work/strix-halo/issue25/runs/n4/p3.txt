We need answer user: "Implement an LRU cache in Rust with O(1) get and put. Explain the data structure choices and show tests."

Need produce final with Rust code, explanation, tests. Need likely include HashMap and doubly linked list. Need O(1) get/put. Need maybe use std::collections::HashMap and custom doubly linked list with Node { key, value, prev, next } using Rc<RefCell<Node>>? But for O(1) and safe Rust, can use HashMap<K, NodeId> and intrusive linked list? Simpler: HashMap<K, (V, NodeIndex)> and VecDeque? But O(1) removal arbitrary requires doubly linked list. In Rust, can implement with HashMap<K, Node> where Node contains Rc<RefCell<...>>? Need avoid borrow issues.

Options:
1. Use `HashMap<K, Rc<RefCell<Node<V>>>>` and head/tail as Option<Rc<RefCell<Node<V>>>>. Node has key, value, prev, next. get: map.get(key) returns Option<&Rc<RefCell<Node>>>, clone Rc, lock RefCell, move_to_front. put: if exists update value and move_to_front; else insert new node at front, if capacity exceeded remove tail, map.remove(&tail.key). Need key stored in node to remove from map. This is straightforward but uses heap allocation per node and RefCell. O(1) amortized? HashMap O(1) average. Need mention average due to hashing.

Potential issue: `get` returns Option<&V> but if we move node to front, the reference to value? We can clone Rc, then `let node = node.borrow();` but if we return `&node.value`, borrow of RefCell must live as long as returned reference, but we also need to mutate list (move_to_front) which requires mutable borrow. Can't return reference while holding mutable borrow. Could return owned clone if V: Clone? But LRU cache get usually returns Option<&V>. Need design to allow returning reference while updating recency.

Alternative: Use `HashMap<K, NodeId>` and a custom doubly linked list stored in `Vec<Node<V>>` with indices. Then get can return `&V` from Vec while updating indices? Need mutable access to list and map. If we have `&mut self`, we can update list then return reference? In Rust, cannot return reference to self after mutable operations? Actually can: `fn get(&mut self, key: &K) -> Option<&V>` can mutate self then return `&self.list[idx].value`? Need lifetime tied to &mut self? The returned reference borrows self immutably? Let's recall: `fn get(&mut self, key: &K) -> Option<&V>` with elided lifetime: `fn get<'a>(&'a mut self, key: &K) -> Option<&'a V>`? Is that allowed? Yes, a mutable borrow can be reborrowed as immutable for returned reference? Example: `fn get_mut(&mut self, i: usize) -> Option<&mut V>` returns mutable. For immutable, `fn get(&mut self, key: &K) -> Option<&V>` is allowed? I think yes: `&mut self` can produce `&V` with lifetime of `&
