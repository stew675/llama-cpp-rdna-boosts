We need answer user: "Implement an LRU cache in Rust with O(1) get and put. Explain the data structure choices and show tests."

Need produce final with Rust code, explanation, tests. Need likely include HashMap and doubly linked list. Need O(1) get/put. Need maybe use std::collections::HashMap and custom doubly linked list with Node { key, value, prev, next } using Rc<RefCell<Node>>? But for O(1) and safe Rust, can use HashMap<K, NodeId> and intrusive linked list? Simpler: HashMap<K, (V, NodeIndex)> and VecDeque? But O(1) removal arbitrary requires doubly linked list. In Rust, can implement with HashMap<K, Node> where Node contains Rc<RefCell<...>>? Need avoid borrow issues.

Options:
1. Use `HashMap<K, Rc<RefCell<Node<V>>>>` and head/tail as Option<Rc<RefCell<Node<V>>>>. Node has key, value, prev, next. get: map.get(key) returns Option<&Rc<RefCell<Node>>>, clone Rc, lock RefCell, move_to_front. put: if exists update value and move_to_front; else insert new node at front, if capacity exceeded remove tail, map.remove(&tail.key). Need key stored in node to remove from map. This is straightforward but uses heap allocation per node and RefCell. O(1) amortized? HashMap O(1) average. Need mention if K: Eq + Hash, V: Clone? For get maybe return Option<&V> or Option<V>? If return reference, lifetime tied to cache, but moving node to front changes Rc but not value location? With RefCell, can return &V from borrow? But moving to front mutates node's prev/next, not value. Could return Option<&V> by borrowing node.value after move_to_front? But move_to_front borrows node mutably, then return immutable borrow? Need manage. Simpler return Option<V> requiring V: Clone. But LRU cache often get returns Option<&V>. Could implement with `HashMap<K, NodeId>` and separate `Vec<Node>`? But moving nodes in Vec O(n). Could use linked list with indices and arrays for prev/next, values, keys. That can return references? Hard due to reallocation.

Alternative: Use `HashMap<K, usize>` and custom doubly linked list using `Vec<Option<usize>>` for prev/next, `Vec<Option<K>>`, `Vec<Option<V>>`, free list. O(1) move by updating indices. But returning reference to value: if Vec reallocation on push, references invalid. Could preallocate capacity and use free list, no reallocation after init. Then can return &V safely? Need ensure no mutation of Vec while reference alive. get mutates prev/next but not values vector? It may mutate prev/next arrays, not values. If return &V from values[index], and then caller holds while put may mutate values? If put updates existing value, it mutates values[index] while reference may be alive -> borrow checker prevents if method returns &V and also mutates self? Actually `fn get(&mut self, key) -> Option<&V>` can return reference to self.values[idx]. It borrows self mutably? Returning &V from &mut self yields lifetime
