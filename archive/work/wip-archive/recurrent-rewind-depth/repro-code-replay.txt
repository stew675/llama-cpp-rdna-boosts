Write a complete, production-quality Python module that implements a small in-memory key-value store.
It must be long: give every class and function full type hints and a docstring, include a small
persistence layer, a TTL/expiry mechanism, a transaction/rollback API, an LRU eviction policy, a
command-line interface, and an extensive `if __name__ == "__main__":` self-test section. Do not
abbreviate anything and do not use "..." placeholders. Name the main class `Store`.

After the module is complete, output the ENTIRE module again, exactly as written, but with the class
`Store` renamed to `Database` everywhere, including in the docstrings and the self-tests. Output the
full second copy in full, character for character. Do not summarise it, do not say "same as above",
and do not skip any function. Start the second copy with the line `# ---- copy 2: Store renamed to Database ----`.
