We need answer user: "Explain in detail how a hash map works internally, including collision handling, load factor, and resizing. Then write a complete implementation in Python."

Need produce final with detailed explanation and complete Python implementation. Need likely include open addressing vs chaining. User asks hash map internally, collision handling, load factor, resizing. Then complete implementation in Python. We can implement using separate chaining or open addressing. Need complete implementation maybe class HashMap with get, put, delete, contains, keys, values, items, len, iter, __getitem__, __setitem__, __delitem__, __contains__, __repr__, maybe clear. Need discuss load factor and resizing. Could implement separate chaining with lists of entries, dynamic resizing. Or open addressing with linear probing and tombstones. Which is better? Complete implementation in Python. Need be careful with hash collisions, equality, mutable keys? We can mention keys must be hashable. Need maybe use Python's hash and equality. Need handle
