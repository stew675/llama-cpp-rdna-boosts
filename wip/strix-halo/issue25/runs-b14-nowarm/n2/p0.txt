We need answer user: "Explain in detail how a hash map works internally, including collision handling, load factor, and resizing. Then write a complete implementation in Python."

Need produce final with detailed explanation and complete Python implementation. Need likely include open addressing vs chaining. User asks hash map internally, collision handling, load factor, resizing. Then complete implementation in Python. We can implement using separate chaining or open addressing. Need complete implementation maybe class HashMap with get, put, delete, contains, keys, values, items, len, iter, __getitem__, __setitem__, __delitem__, __contains__, __repr__, maybe clear. Need discuss load factor and resizing. Could implement separate chaining with lists of entries, dynamic resizing. Or open addressing with linear probing and tombstones. Which is better? Complete implementation in Python. Need be careful with hash collisions, equality, mutable keys? We can mention keys must be hashable. Need maybe use Python's hash and equality. Need handle negative hash modulo. Need load factor threshold e.g. 0.75. Resize when size > capacity * load_factor. Initial capacity maybe 16. Use power of two? For separate chaining, capacity can be any int, but power of two helps if using mask. But if using list of buckets, can use modulo. For open addressing, power of two and mask. Need choose one.

The prompt: "Explain in detail how a hash map works internally, including collision handling, load factor, and resizing. Then write a complete implementation in Python." It doesn't specify collision handling method. We can explain both, then implement one. Maybe implement separate chaining because simpler and robust. But "complete implementation" maybe should include collision handling and resizing. Separate chaining: array of buckets, each bucket linked list or Python list of (key,value). Hash key -> index = hash(key) % capacity. If collision, append to bucket. Lookup scans bucket. Load factor = number of entries / capacity. When exceeds threshold, resize to double capacity, rehash all entries into new buckets. Deletion: remove from bucket; no tombstones needed. Complexity: average O(1), worst O(n) if all collide or bad hash. Need mention Python dict uses open addressing with perturb algorithm, but our implementation can use chaining.

Need maybe include tests? Complete implementation could include if __name__ == '__main__': simple tests. Need ensure code correct.

Let's design HashMap:

class HashMap:
    def __init__(self, initial_capacity=16, load_factor=0.75):
        if initial_capacity <=0: raise ValueError
        if not (0 < load_factor <=1): maybe allow >1? Usually 0<lf. We'll require >0.
        self._capacity = initial_capacity
        self._load_factor = load_factor
        self._size = 0
        self._buckets = [[] for _ in range(self._capacity)]

    def _index(self, key):
        return hash(key) % self._capacity

    def _find_bucket(self, key):
        return self._buckets[self._index(key)]

    def _find_entry(self, key):
        bucket = self._buckets[self._index(key)]
        for i, (k, v) in enumerate(bucket):
            if k == key:
                return i, k, v

