Continue the Strix Halo prefill work. START by reading, in order: (1) ~/make-strix-halo-faster.md (the plan; WS1/WS2 done, WS3 recommendation sent, WS4 = port of B's prefill hyperconn fusions into A is IMPLEMENTED + DEFAULT ON), (2) ~/llama-cpp-rdna-boosts/wip/strix-halo/notes-ws1-survey.md (full evidence trail), (3) ~/llama-cpp-rdna-boosts/wip/strix-halo/PORT-hc-prefill.md. Then do the next-session priorities below IN ORDER.

STATE: ~/llama.cpp (branch qwen4exp, commit dca0526a8) has UNCOMMITTED work: new files ggml/src/ggml-cuda/hyperconn.cu + hyperconn.cuh (B's kernels, RN-completed, host asserts relaxed for the 2D xn output) and edits to ggml/src/ggml-cuda/ggml-cuda.cu (include, ggml_cuda_match_hc_mix, two try_fuse blocks adapted to A's graph: broadcast-MUL/no-REPEAT, view-tolerant scans, gamma = non-rms operand of the post-rms MUL, block_out = repeat input, alias checks vs bo+gamma, full-node-window can_fuse_subgraph_ext; env gates GGML_CUDA_DISABLE_HC_FUSION/_HC_MIX/_HC_COMB; DEFAULT ON; debug prints removed; build clean via ~/bin/build-llama-rocm-714 = build-rocm). Kernels fire 190x (hc_mix_reduce) + 186x (hc_combine_norm) per 2048-ubatch on IQ4_XS; +6-7% pp2048-8192; dispatch count 10585->8519.

CRITICAL FACTS:
- This box + Qwen3.8-Flash-Next + f16/bf16 KV is run-to-run NONDETERMINISTIC at the logits level (llama-cli AND llama_decode harnesses; 5/5 runs differ; fusion-off too; CPU-pin doesn't help). Root cause: A LACKS B's kv-cache stale-cell zeroing (halo-box/strix-llama.cpp commit aad5adb08: on RDNA f16 FA, x+(-0.0) inexact -> masked cells leak stale V sign from allocator garbage; B's commit names this exact model "5-6 distinct outputs"). => NO per-process determinism comparisons are trustworthy until that fix is ported. Do not debug fusion "divergence" via llama-cli text on this box pre-fix; use the libllama harness /tmp/logitcmp.cpp (prompt + N greedy steps, per-step logits FNV hashed; llama_get_logits, single-output batches) and only compare within deterministic conditions.
- The hc fusion itself was proven bit-identical to the unfused path over 45 hashed logit positions (all configs equal) before the substrate degraded; arithmetic equality is structural (RN asm on every op with a graph counterpart). Re-verify after the KV fix.

NEXT-SESSION PRIORITIES IN ORDER:
1. PORT B's kv-cache stale-cell fix (strix-llama.cpp aad5adb08 -> ~/llama.cpp src/llama-kv-cache.cpp: zero-on-free in seq_rm/seq_keep/clear + sharers + rows_hw). Validate with B's gate: 16 identical greedy requests -> 16/16 identical; fresh-server output byte-identical. This restores deterministic same-seed execution (coherence-gate validity) on RDNA f16/bf16 KV.
2. RE-VERIFY hc-fusion bit-exactness on the now-deterministic substrate: harness /tmp/logitcmp.cpp, default vs GGML_CUDA_DISABLE_HC_FUSION=1, several prompts incl. deep context (pp >= 4096 at depth via llama_decode, and 40+ decode steps). Expect PASS.
3. FINISH WS4 GATES: llama-cli same-seed coherence (now meaningful); clean warm-clock r3 pp ladder depth 0 (descending order, pp16384 warmup first; see plan for the exact command) + depth 12k/32k rows; decode tg@depth regression (must not regress; fusion is prefill-only by the ne[1]==1 guard); memory-stability ladder at -r3 through depth 32k (watch for crashes/leaks - B crashed there in ITS argsort/CUB path; ours adds no CUB).
4. RECORD + ROUTE: dated bench file benchmarks/2026-09-05-strix-halo-hc-fusion.md (mirror the block-13 record), delivery routing per AGENTS (qwen4exp tree -> beta patch or block amendment; NEVER push from ~/llama.cpp), update ~/llama-cpp-rdna-boosts/TODO.md.
5. RE-DERIVE remaining A-vs-B gap after the fusion; then WS3 #2 (dense shortcut below selection width in A's build_layer_attn - recovers shallow decode/small-pp; fires only while n_kv <= top_k+ratio-1) and #3 (routed-compact MoE mmq, RDNA4-gated + validation) as follow-ons; gfx1201 validation of everything via the normal delivery flow. WS6 re-base still NOT indicated.

HYGIENE/RULES (all in AGENTS.md + plan §9): no parallel/background benches; warm page cache before runs; first test after process start = long pp warmup (cold GPU clock); one server at a time (port 8033); ~116 GB effective VRAM, ~420 GB disk; push policy: NEVER push from ~/llama.cpp or to upstream/halo-box - delivery repo only. Save scratch in ~/llama-cpp-rdna-boosts/wip/strix-halo/.
