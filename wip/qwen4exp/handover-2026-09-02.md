# HANDOVER 2026-09-02: qwen4exp state, branch layout, captured diffs, next work

Status: repo setup for ITEM B optimization. Context-safe to compact after
reading this.

## 1. Repo / branch layout (CURRENT, verified 2026-09-02)

| repo | branch | tip | contents |
|------|--------|-----|----------|
| `~/llama.cpp` | `rdna-boosts` | `482837e5a` | 0eadefebd + blocks 01-13 (delivery set, DO NOT pollute) |
| `~/llama.cpp` | `qwen4exp` | `0ff151bda` | rdna-boosts + ITEM B re-applied (WORK HERE) |
| `~/prs/llama.cpp` | `qwen4exp` | `554691a72` | OLD pre-re-base tree (reference/backup only) |
| `~/llama-cpp-rdna-boosts` | `main` | `6cc5830` | delivery repo; wip/ holds qwen4exp + managed-ngrams |

- qwen4exp work stays on `qwen4exp` branch ONLY; never commit to
  `rdna-boosts` (that is the 13-patch delivery branch).
- `0ff151bda` = `482837e5a` + one commit (the ITEM B re-apply). Clean tree.

## 2. ITEM B status (the active work)

- **DONE**: `GGML_OP_FLASH_ATTN_QSA` sparse flash-attention op + tiled
  all-heads-per-block kernel. Attention is depth-constant (11.9ms @64K vs
  dense 158ms). Curve @16K..64K: sparse 1857/1880/1836/1718/1549 vs dense
  2028/1935/1716/1381/995 (+56% at 64K). Correctness inside the
  tile-vs-wmma envelope (max diff 6.50, top-1 84%).
- **REMAINING (the next task)**: latency optimization. Kernel is
  latency-bound on the per-warp serial score->softmax->VKQ chain (~35%
  GPU, ~3.5% of compute peak, 12 warps walk the same cells independently).
  Levers (in `ggml/src/ggml-cuda/fattn-qsa.cu`):
  - a) stage K/V tiles in shared memory (tile-FA style) - heads read LDS
  - b) software-pipeline the score pass (prefetch next K tile)
  - c) 2 columns/block (grid.x halved, more warps)
  - Expected ceiling if latency fully hidden: ~3x (~5000+ t/s at 16K).
  - Re-verify after each change: logitdump envelope (max diff / top-1) +
    the depth curve (pp16384/32768/65536).
- Env toggles: `LLAMA_QSA_SPARSE_FA=1` (enable sparse),
  `GGML_CUDA_QSA_IDENTITY=1` (validation), `GGML_CUDA_FA_WMMA_256=0`
  (docs-era benches used WMMA off).
- Re-apply record: `handoff-2026-09-02-rebase-reapply.md` (one conflict
  resolved: ggml-backend.cpp keeps upstream MUL_MAT + new FLASH_ATTN_QSA).

## 3. Bench config (MUST follow; the "regression" was a harness artifact)

Full canonical commands: `wip/qwen35moe-prefill/bench-config.md`.
- qwen4exp model: `/models/Qwen3.8/Flash-Next/Q4_K_XL/Qwen3.8-Flash-Next-UD-Q4_K_XL-00001-of-00004.gguf`
  (4 shards, 103.68 GiB, needs ALL 3 GPUs).
- ALWAYS: `HIP_VISIBLE_DEVICES=0,1,2 ... --split-mode tensor` (default
  layer-split can't fit / wrong numbers).
- WARM UP 2-3 pp runs before recording; report GPU temps. The docs-era
  1716 t/s was a WARM machine; cold starts climb 1210->1591 as GPUs warm
  to 60-61C (manual DPM mode, -70mV/250W via tune_r9700.sh).
- `-r 3` matches docs-era. tg/decode: `-p 512 -n 128 -ub 2048 -r 2`.
- /tmp is tmpfs: NEVER write logitdump bins to /tmp (16 GB each) - use
  /home or delete after.

## 4. Captured diffs inventory (what survives compaction)

`wip/qwen4exp/patches/`:
- **`0008-item-b-qsa-sparse-flash-attn.patch`** (NEW 2026-09-02): the
  whole ITEM B op+kernel (9 files, 741 lines, applies clean to
  rdna-boosts). This was the missing capture - now done.
- `0005-kv-prev-tokens-index.patch`: PR #27992-style prev-token index
  (OUR implementation: `seq_pos_token_le` + `get_prev_tokens_indexed` +
  FAST/VERIFY/OFF modes + unit test). Applies clean to current fork.
  NOTE: the base `seq_pos` map is ALREADY upstream (came in via #27991
  in the re-base); 0005 adds the query side on top.
- `0006-qsa-topk-radix-gpu.patch`: historical ONLY - superseded by
  upstream #27466 (radix top-k) which landed in the re-base; top-k.cu is
  now identical. Do not re-apply.
- `0007-ple-batch-cache-archive.patch`: archived neutral prototype.

`wip/managed-ngrams/patches/0001-0007`: lazy-reader / PLE managed ngram
loading (separate workstream, captures llama-lazy-reader.cpp/h,
llama-model-loader, LLAMA_UBATCH_TIMING, tests). NOT needed for ITEM B.

## 5. PR #27992 / prev-token index - the user's memory (confirmed)

Yes - we rolled our own indexed version. It is captured (0005) and
applies clean. Key nuance from the 08-31 handoff: at 16K ctx the index is
FLAT vs the general scan (683 vs 692 t/s); the win is at 240K ctx (2.7x
decode). The 16K CPU burn that LOOKED like get_prev_tokens was actually
per-ubatch host overhead (graph scheduler + gallocr reserve + the 27.4 GB
ngram GET_ROWS gather spinning 16 OpenMP threads). So for the 16K ITEM B
work, the prev-token index is NOT the limiter; for 240K it matters.

## 6. Open / parked items (from the 08-31 handoffs)

1. ITEM B latency pass (section 2) - THE next task.
2. Indexer pipeline depth falloff: SUM_ROWS + CONT x3 + TOP_K scale with
   n_kv (12.7 + 10.6 + 7.2ms at 64K) - the dense path pays these too.
   Head-sum fusion attempt (GGML_OP_INDEXER_HEAD_SUM) was REVERTED (perf
   regressed from graph-splitter perturbation - lesson: measure
   end-to-end, not op timing).
3. 240K ctx: prev-token index (0005) + managed-ngrams matter here.
4. Model-family port: the QSA op is model-agnostic (deepseek4 LID,
   deepseek32 DSA, glm-dsa, dflash, minimax-m3, eagle3 produce the same
   indexer->top-k->mask inputs). Upstream story.
5. Strix Halo validation (parked - needs hardware).
6. Decode ~40 t/s: bandwidth-bound (103.68 GiB weights), separate from
   prefill.

## 7. Machine state notes

- 3x R9700 gfx1201, ROCm `/opt/rocm-7.14-gfx1201` (build AND runtime).
- Runtime PM: `power/control=on` on 0000:{03,06,09}:00.0 (applied).
- GPUs in `manual` DPM (tune_r9700.sh: -70mV, 250W). sclk S:0->500->2350.
  The nvtop stop-start (80% spikes + idle gaps) is the manual-mode clock
  ramp between ubatch bursts - expected, not a bug.
- Free RAM ~61G + 115G page cache; the 103 GiB model + 27 GB ngram table
  fit if cache is warm. Keep /tmp clean (tmpfs).
- OLD tree at ~/prs is a working A/B reference (binaries built from
  554691a72) - op-for-op identical to current fork for the dense path.
