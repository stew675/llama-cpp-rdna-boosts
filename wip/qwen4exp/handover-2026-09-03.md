# HANDOVER 2026-09-03: qwen4exp - MoE redundant-compute elimination (the next work)

Status: prefill push, next task = the packed MoE with a compile-time J_LEN
vec_dot. Context-safe to compact after reading this (supersedes
handover-2026-09-02.md, which remains as the full session history).

## 0. CURRENT GOAL (revised): prefill 2250 t/s at pp8192

**THE GOAL**: reach **2250 t/s prefill at pp8192** with Q4_K_XL on 3x R9700
(current: ~2000-2010 t/s warm). Target revised from 3000: the earlier goal
was set against an external 4000 t/s pp8192 claim on 2x R9700 that cannot be
verified (no public source or benchmark was ever provided). Until further
information is provided, we go with what we have measured. The measured
reference point we DO have: R9V's qualified dual-R9700 vLLM engine (custom
group-16 MoE kernels, AITER, fusions) does 1512 t/s pp8192 = 756/GPU; our
fork does ~666/GPU. All targets in this session are grounded in our own
measurements only.

**The path to 2250** (aggregate of the quantified levers, in descending
order of confidence):
1. **[NEXT TASK] MoE redundant-dot elimination: ~+12%** (this document).
2. Larger ubatch (>2048): ~+10%, but memory-constrained (pinned - try LAST).
3. QSA kernel (10% of wall): ~+6-7% ceiling, high regression risk (history).
4. Tail/hc machinery (53% of wall): diffuse, mostly bandwidth-floor.

## 1. Repo / state (verified 2026-09-03)

| repo | branch | tip | contents |
|------|--------|-----|----------|
| `~/llama.cpp` | `qwen4exp` | `9dff5cc4f` | rdna-boosts + ITEM B + Q8_0 KV + fused indexer top-k (WORK HERE, CLEAN) |
| `~/llama.cpp` | `rdna-boosts` | `482837e5a` | 13-patch delivery branch (DO NOT pollute) |
| `~/llama-cpp-rdna-boosts` | `main` | `cdcc0af` | delivery repo; wip/qwen4exp/ holds the handovers |

- Work ONLY on `qwen4exp`. Working tree is CLEAN at 9dff5cc4f (verified).
- Micro-bench sources persisted at `wip/qwen4exp/tools/mmq_bench.cpp` +
  `ew_bench.cpp`; live copies in /tmp (mmq_bench*.cpp variants from this
  session: mmq_bench_moe, mmq_bench_msweep, mone_* etc. - tmpfs, re-derive
  from the persisted copy if lost).

## 2. THE TASK: eliminate the MoE redundant dot compute (2.2-2.6x waste)

### The mechanism (fully measured this session - trust this, not older notes)
- clock64 phase profile of the mul_mat_q K-loop (baseline kernel, per block
  of 10 K-iters, steady state):
    x load (load_tiles) ~25K cyc (20%) | y staging ~30K (20%) |
    vec_dot ~85-110K (55-65%) | post-load barrier wait ~5-10K (3-7%)
  => NOT waiting on data at the barriers (the earlier "latency-bound" model
  was WRONG for the MoE). The vec_dot dominates because EVERY block dots the
  FULL J-row tile regardless of the expert's real row count.
- Dense M=20480 has the identical per-block phases (~74K dot); the MoE just
  runs 3.2x more blocks (one full-tile block per expert) because the J-tile
  grid is sized from ncols_max=20480 while experts have few rows each.
- Measured REAL routing (pp2048, ub=2048, expert_bounds dump): 362/512
  experts used, Zipf-like (one expert 1077 rows; 34 experts >128 rows;
  ~110 experts <=8 rows). Exact dot-row waste:
    J=96  (fused gate): 2.18x | J=128 (down): 2.59x | aligned-16 floor: 1.17x
  The synthetic uniform 512x40 bench (3.2x) OVERSTATES the real waste.
- Expected win: dot phase ~60% of block drops ~2x (53120 -> 23920 dot-rows),
  stage drops ~2x (guarded staging), load stays (weight-bound, irreducible).
  MoE wall share 24.6% -> ~11-13% -> pp8192 ~2250 t/s.

### THE CRUX (why the previous attempt failed - do not repeat blindly)
The full packing scaffold was implemented and REVERTED this session (see
handover-2026-09-02.md "UPDATE ... MoE packing experiment"). It failed ONLY
because the runtime-bounded j0 loop does NOT make the MMA skip:
- j_len=48 vs full-J A/B measured 6.42 vs 7.05ms = 9% savings (expected 2.7x).
- Cause: `#pragma unroll` on the runtime-bounded j0 loop emits the full
  J/16-group body with only the ACCUMULATION guarded; the B-loads and MMA
  still run for all groups.
- Removing the pragma made it WORSE (6.75ms) - a naive dynamic loop cannot
  pipeline the ldmatrix+mma chain across the unknown trip count.
- The J-sweep flatness (16..128 all ~6.1ms) is explained by the config
  coupling (I and nthreads scale with J) + the J-independent load+stage.

### The fix: compile-time J_LEN templating (the actual design)
1. Add a template param `int J_LEN` to the vec_dot implementations used by
   the MoE (start with `ggml_cuda_mmq_vec_dot_q8_1_q8_1_mma` in
   mmq-vec-dot.cuh - serves BOTH Q4_K and Q5_1) and bound the j0 loop with
   the CONSTEXPR J_LEN: `for (int j0 = 0; j0 < J_LEN; j0 += ntx*tile_C::J)`.
   The constexpr bound gives the compiler the EXACT trip count -> full
   unroll -> the MMA genuinely shrinks. The y/sum pointer offsets stay the
   caller's business (rows [0, J_LEN) of the given pointers).
2. In process_tile, dispatch the runtime j_len to the compile-time
   instantiations: a switch over the classes {16, 32, 48, 64, 80, 96, 112,
   128(=J)} calling the J_LEN-specialized vec_dot. Limit the instantiation
   set to the classes that occur (j_len = (rows_p+15)&~15, capped at J) to
   bound the template blowup. VERIFY the mma skip FIRST via the micro-bench
   j_len A/B: the synthetic 512x40 gate should drop 6.07 -> ~2.5-3ms (a
   real ~2.2x, not 9%).
3. Re-apply the packing scaffold on top (all in the 09-02 handover record;
   the reverted diffs were: mmq.cuh + mmq-vec-dot.cuh + mmq-config-rdna4.cuh):
   - process_tile per-expert/pass loop (sequential experts per block, pad
     rows zeroed, j_len = min(aligned(rows_p), J), write-back j_max guard
     unchanged, ids_dst reload guarded by j < rows_p - the M=128 empty-
     expert fault came from an unguarded reload in that experiment).
   - kernel ids branch: zt = expert group of MMQ_MOE_EXPERTS_PER_BLOCK=4,
     grid.y=1, offset_x without the zt term (per-expert + e*stride_channel_x).
   - launch: packed_ids grid (nty, 1, ceil(nchannels/E)) when ids_dst &&
     !stream_k. Keep the J at 96/128 (the ntiles heuristic) - do NOT add
     the J=64/I=64 configs (they faulted on the real model this session -
     likely a separate pre-existing small-tile issue).
4. Correctness gate BEFORE any perf claims: logitdump A/B (see section 4).
   Expected: bit-identical or near (per-row K-accumulation order unchanged).
   Known small-M baseline faults (M<256 MoE OOB in ids_dst fill) are PRE-
   EXISTING upstream bugs, not from this work (relevant if testing small M).
5. Measure pp8192 (warm) A/B. Target: ~2250 t/s (from ~2000).

### Known traps (from this session's experiments - all real)
- The vec_dot j_len A/B (9%) is the codegen tell. Verify the skip before
  any perf measurement.
- The J=64/I=64 config faulted at small M AND the real model hung with the
  fused gate in the packed path (unroot-caused; the fused path needs a
  bench reproducer - the GLU pattern - before any fused-gate packing).
- M=128 synthetic routing faults in the packed path (empty-expert groups).
- rocprofv3 HANGS on this workload; the micro-bench harness is the tool.
- The old "stream-k" and "J-sweep" conclusions in the 09-02 handover are
  superseded by the phase profile (this session's dot-redundancy model).

## 3. Bench config (MUST follow)

- Model: /models/Qwen3.8/Flash-Next/Q4_K_XL/Qwen3.8-Flash-Next-UD-Q4_K_XL-00001-of-00004.gguf (4 shards, 103.68 GiB, 3 GPUs).
- ALWAYS: `HIP_VISIBLE_DEVICES=0,1,2 ... --split-mode tensor`; env
  `GGML_CUDA_FA_WMMA_256=0 LLAMA_QSA_SPARSE_FA=1`; `-t 16`; record `-ub`.
- WARM 2-3 pp runs before recording; report temps (manual DPM, 60C+).
- pp8192 current warm: ~2000-2011 t/s (ub=2048). Depth curve (sparse warm):
  16K 1970, 32K 1902, 64K 1801, 128K 1200 (Q8_0 KV, ub=512).
- `-ub` clamps to `-b` (2048); `-b 8192` crashes context creation.
  ub=16 pp1024 = 148 t/s (12.5x worse) - small ubatches are catastrophic.
- Real model MoE: fused gate+up Q4_K (J capped 96, J_max_gate table), down
  Q5_1 (J=128), 362/512 experts used at M=2048, waste 2.18x/2.59x.
- Micro-bench: build against build-rocm/bin libggml*.so + ROCm libs;
  LD_LIBRARY_PATH=/opt/rocm-7.14-gfx1201/lib:$HOME/llama.cpp/build-rocm/bin.
  Key shapes: moe_gate_up Q4_K [2560,1280,512] x [2560,2048] ids[10,2048]
  (synthetic 512x40: 6.07ms base, 11.1 TFLOPS); moe_down Q5_1 [640,2560,512]
  (5.9ms base); moe_fat (10 exp x 2048 rows: 1.05ms, 60 TFLOPS - full tiles);
  dense Q4_K [2560,1280] M=20480 (32.5 TFLOPS). The real routing histogram
  dump recipe (GGML_CUDA_DUMP_MOE env in mmq.cu) is in this session's record.

## 4. Correctness procedure (logitdump A/B)

- Build: bash /tmp/build_logitdump.sh (uses /tmp/logitdump_min.cpp, links
  the current .so). The parser wants `--split-mode 1` (atoi, not "tensor").
- Baseline dump exists: /home/ld1024_base.bin (HEAD 9dff5cc4f, -n 1024).
  Run the new build's dump to /home/ld1024_new.bin, then compare top-1 +
  max diff. Expect bit-identical for the MoE rows (accumulation order per
  row unchanged by the packing). Note: memory - kill stale llama-bench
  processes before runs (they hold the GPUs); the model load takes ~3 min.

## 5. State summaries (condensed from the 09-02 handover)

- Model: qwen4exp, 48 layers = 12 QSA (indexer) + 36 linear-attn
  (GATED_DELTA_NET, no FLASH_ATTN); hc=4 streams; 512 experts x 640 hidden,
  top-10 used; "UD-Q4_K_XL" = unsloth mixed-quant (Q4_K gate/up + Q5_1 down
  + Q8_0 hc/attn + F32 router; no Q4_K_XL format exists).
- Weight types: MoE gate/up Q4_K (J_max_gate 96), down Q5_1 (J 128);
  GGUF enum Q8_0=8, Q8_1=9 (corrected parser).
- Per-GPU-ubatch budget pp8192 (4112ms wall, ub=2048): MoE 24.6% (down 507 +
  fused gate 504ms, 192 invocations ~2.64ms each) | QSA 10% | hc ~12.6% |
  tail ~53%. R9700 BW ~640 GB/s (F32 elementwise at 55-90% BW = not slow).
- Bench harness facts: op timing inflates ops by a per-op sync ~47-180us;
  big single ops (QSA, MUL_MAT_ID) near-real; per-ubatch wall depth-constant
  (~1.03s at M=2048).
- Prior wins (in the tree): indexer fused top-k + radix bpr=8, QSA latency
  pass (7b5a94cca, abc4b13ee), Q8_0 KV, 128K depth (1200 t/s), all prior
  qwen35moe work.
- Decode: ~37 t/s (bandwidth-bound), parked.
- Machine: 3x R9700 gfx1201, ROCm /opt/rocm-7.14-gfx1201, manual DPM.
- R9V repo (~/R9V): colleague's verified vLLM reference (1512 t/s pp8192 on
  2x R9700 with ud-iq4-xs). We are ~88% of it per GPU with a heavier quant.
  Its group-16 MoE kernel mechanism lives in a private vLLM fork (not
  checked out); treat its knobs as ideas, not measured wins for us.
