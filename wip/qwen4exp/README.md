# qwen4exp WIP - archived

The qwen4exp support work was promoted to BETA status on 2026-09-03.

- **Promoted baseline:** `../../beta/qwen4exp/` - two squashed patches
  (`managed-ngrams.patch`, `qwen4exp-support.patch`) + a README with the
  validation gates and the carried-forward open-items list.
- **This directory** now only holds `archive/`: the full WIP history
  (per-commit patch files with notes, handoff/handover docs, plans,
  micro-bench tools, the rebase record) preserved for reference.

The items in `archive/` were promoted to the beta directory as the pair
of squashed patches; see `beta/qwen4exp/README.md` for the open items
that remain TODO.
